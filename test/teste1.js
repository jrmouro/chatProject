const expect = require('chai').expect;


describe('teste1',() => {
    it('é só um teste',() => {
        expect(0).to.equal(0);
    });
    it('é só um teste a mais ',() => {
        expect(0).to.equal(0);
    });
});